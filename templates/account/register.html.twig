{% extends "base.html.twig" %}
{% block title %}Inscription {% endblock %}

{% block body %}

<div class="container">
        <h1 class="text-center">Créer votre compte par ici </h1><br>
        <div class="card">
         <div class="card-body">
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_errors(form)}}
        <button type="submit" class="btn btn-primary">Confirmer</button>

        {{form_end(form)}}

       
        </div>
        </div>
     </div>

{% endblock %}